import 'package:flutter/material.dart';

class ColorsManager {
  // لون الخلفية الأساسي (الكحلي الداكن الفخم)
  static const Color mainDarkBlue = Color(0xFF000826);

  // اللون الثانوي المضيء (التركواز / الأزرق النيون) - يستخدم للأزرار والأسعار
  static const Color neonBlue = Color(0xFF007BFF);
  static const Color lightBlue = Color(0xFF00D4FF);

  // ألوان النصوص
  static const Color white = Color(0xFFFFFFFF);
  static const Color lightGray = Color(0xFFE0E0E0);
  static const Color darkGray = Color(0xFF757575);

  // ألوان الحالات (مثلاً: نجاح الطلب، خطأ، قيد الانتظار)
  static const Color successGreen = Color(0xFF00C853);
  static const Color errorRed = Color(0xFFD32F2F);
  static const Color warningYellow = Color(0xFFFFA000);
}